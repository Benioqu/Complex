//deklaracja funkcji i struktury

struct complex
{
	double re;
	double im;
};

void print(complex z);

complex add(complex z1, complex z2);
complex substract(complex z1, complex z2);
complex multiply(complex z1, complex z2);
complex divide(complex z1, complex z2);
